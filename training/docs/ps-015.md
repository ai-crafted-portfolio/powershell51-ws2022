# Windows 研修資料 No.15 正規表現（-match/-replace/[regex]）

### Windows PowerShell 5.1
正規表現（-match/-replace/[regex]）
2. 言語要素・構文　>　文字列　>　正規表現
PowerShell5.1 技術項目　No.15
凡例：・＝要点／－＝補足　｜　枠線囲みのMS Gothic＝コマンド・設定　｜　出典はMicrosoft Learn公式（末尾スライドにURL）。値・パスは標準既定の例で実環境確認が必要。

### 正規表現（-match/-replace/[regex]）
2. 言語要素・構文　>　文字列　>　正規表現
PowerShell5.1 技術項目　No.15　／　2. 言語要素・構文
この研修シリーズでの位置づけ
・1. PowerShell基盤・実行環境
・▶ 2. 言語要素・構文　（この項目が属する分類）
・3. パイプラインとオブジェクト処理
・4. スクリプティングと関数
・5. モジュールとプロバイダ
・6. リモート処理・CIM/WMI
・7. ジョブ・スケジュール・ワークフロー
・8. データ入出力・変換
・9. セキュリティ
・10. 診断・ログ・監視
・11. 国際化・パッケージ運用

### 正規表現（-match/-replace/[regex]）
2. 言語要素・構文　>　文字列　>　正規表現
PowerShell5.1 技術項目　No.15　／　2. 言語要素・構文
扱う対象と前提
変数・型・演算子・制御構文など、スクリプトを書くための言語そのものの要素を扱う分類です。
パターン照合と置換。$Matches に捕捉結果。
前提
・Windows PowerShell 5.1 標準機能のため追加導入は不要。
・スクリプト実行時は実行ポリシー（例 RemoteSigned）に留意する。

### 正規表現（-match/-replace/[regex]）
2. 言語要素・構文　>　文字列　>　正規表現
PowerShell5.1 技術項目　No.15　／　2. 言語要素・構文
想定作業（この項目で何をするか）
・抽出・置換処理（学習）
・標準的な作業の流れ（参考）：
－現状の設定・構成を確認する（次スライドのコマンド・確認キー）
－必要に応じて作成・変更・削除など目的の操作を行う
－変更後は反映・動作を検証し、記録する
使用判断のポイント
この設定・機能が実環境で使われているかは、下記の確認キーで判断します。使っていなければ研修採用の要否を検討します。

### 正規表現（-match/-replace/[regex]）
2. 言語要素・構文　>　文字列　>　正規表現
PowerShell5.1 技術項目　No.15　／　2. 言語要素・構文
主要コマンド（例）
PS C:\> Get-Command -Module <モジュール名>
確認する値・キー（使用判断）
・-match
・-replace
・[regex]::Matches

### 正規表現（-match/-replace/[regex]）
2. 言語要素・構文　>　文字列　>　正規表現
PowerShell5.1 技術項目　No.15　／　2. 言語要素・構文
定義ファイル・場所（早見）
場所・ファイル・モジュール（標準既定の例）
確認
about_Regular_Expressions
実環境で要確認
補足：パス・既定値はバージョン/構成で異なります。必ず実環境で確認してください。

### 正規表現（-match/-replace/[regex]）
2. 言語要素・構文　>　文字列　>　正規表現
PowerShell5.1 技術項目　No.15　／　2. 言語要素・構文
用語集

### 正規表現（-match/-replace/[regex]）
2. 言語要素・構文　>　文字列　>　正規表現
PowerShell5.1 技術項目　No.15　／　2. 言語要素・構文
出典（Microsoft Learn 公式資料）
・Microsoft Learn（Windows Server 2022 / Windows PowerShell 5.1 ドキュメント）
補足：日本語訳が機械翻訳の頁は英語原文（上記URL）を正とします。社内既存ナレッジ（C:\oskb PS51-WS2022 4,030件）とも整合。
