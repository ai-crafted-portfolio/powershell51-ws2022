# Windows 研修資料 No.09 変数・自動変数・スコープ

### Windows PowerShell 5.1
変数・自動変数・スコープ
2. 言語要素・構文　>　変数　>　変数とスコープ
PowerShell5.1 技術項目　No.9
凡例：・＝要点／－＝補足　｜　枠線囲みのMS Gothic＝コマンド・設定　｜　出典はMicrosoft Learn公式（末尾スライドにURL）。値・パスは標準既定の例で実環境確認が必要。

### 変数・自動変数・スコープ
2. 言語要素・構文　>　変数　>　変数とスコープ
PowerShell5.1 技術項目　No.9　／　2. 言語要素・構文
この研修シリーズでの位置づけ
・1. PowerShell基盤・実行環境
・▶ 2. 言語要素・構文　（この項目が属する分類）
・3. パイプラインとオブジェクト処理
・4. スクリプティングと関数
・5. モジュールとプロバイダ
・6. リモート処理・CIM/WMI
・7. ジョブ・スケジュール・ワークフロー
・8. データ入出力・変換
・9. セキュリティ
・10. 診断・ログ・監視
・11. 国際化・パッケージ運用

### 変数・自動変数・スコープ
2. 言語要素・構文　>　変数　>　変数とスコープ
PowerShell5.1 技術項目　No.9　／　2. 言語要素・構文
扱う対象と前提
変数・型・演算子・制御構文など、スクリプトを書くための言語そのものの要素を扱う分類です。
$名 で変数を扱い、$_,$PSItem,$? などの自動変数がある。
global/script/local/privateのスコープ。
前提
・対象モジュール microsoft.powershell.utility が利用可能であること（多くは初回コマンド実行時に自動読み込み）。
・スクリプト実行時は実行ポリシー（例 RemoteSigned）に留意する。

### 変数・自動変数・スコープ
2. 言語要素・構文　>　変数　>　変数とスコープ
PowerShell5.1 技術項目　No.9　／　2. 言語要素・構文
想定作業（この項目で何をするか）
・変数・スコープの理解（学習）
・標準的な作業の流れ（参考）：
－現状の設定・構成を確認する（次スライドのコマンド・確認キー）
－必要に応じて作成・変更・削除など目的の操作を行う
－変更後は反映・動作を検証し、記録する
使用判断のポイント
この設定・機能が実環境で使われているかは、下記の確認キーで判断します。使っていなければ研修採用の要否を検討します。

### 変数・自動変数・スコープ
2. 言語要素・構文　>　変数　>　変数とスコープ
PowerShell5.1 技術項目　No.9　／　2. 言語要素・構文
主要コマンド（例）
PS C:\> Get-Variable
確認する値・キー（使用判断）
・$var
・$PSItem
・Get-Variable

### 変数・自動変数・スコープ
2. 言語要素・構文　>　変数　>　変数とスコープ
PowerShell5.1 技術項目　No.9　／　2. 言語要素・構文
この項目で使う主なコマンドレット（抜粋）
コマンドレット
主な用途
Get-Variable
取得・確認
Add-Member
追加
Clear-Variable
クリア
Compare-Object
比較
Convert-String
変換
ConvertFrom-Csv
変換(入力)
ConvertTo-Csv
変換(出力)
Debug-Runspace
デバッグ
Disable-PSBreakpoint
無効化
Enable-PSBreakpoint
有効化
Export-Alias
エクスポート

### 変数・自動変数・スコープ（続き）
2. 言語要素・構文　>　変数　>　変数とスコープ
PowerShell5.1 技術項目　No.9　／　2. 言語要素・構文
コマンドレット
主な用途
Format-Custom
書式設定
Group-Object
グループ化
Import-Alias
インポート
補足：上表はモジュールの代表的なコマンドレットの抜粋です。全体は Get-Command -Module <モジュール名> で確認できます。

### 変数・自動変数・スコープ
2. 言語要素・構文　>　変数　>　変数とスコープ
PowerShell5.1 技術項目　No.9　／　2. 言語要素・構文
定義ファイル・場所（早見）
場所・ファイル・モジュール（標準既定の例）
確認
about_Variables
実環境で要確認
about_Scopes
実環境で要確認
補足：パス・既定値はバージョン/構成で異なります。必ず実環境で確認してください。

### 変数・自動変数・スコープ
2. 言語要素・構文　>　変数　>　変数とスコープ
PowerShell5.1 技術項目　No.9　／　2. 言語要素・構文
用語集
用語
区分・変更可否（既定値）
説明
microsoft.powershell.utility
モジュール名（固定）
本項目の操作を提供するモジュール。Import-Module microsoft.powershell.utility で利用。
Get-Variable
コマンドレット
本項目で使用する主要コマンドレット。Get-Help Get-Variable で詳細。

### 変数・自動変数・スコープ
2. 言語要素・構文　>　変数　>　変数とスコープ
PowerShell5.1 技術項目　No.9　／　2. 言語要素・構文
出典（Microsoft Learn 公式資料）
・Get-Variable
https://learn.microsoft.com/ja-jp/powershell/module/microsoft.powershell.utility/get-variable/?view=powershell-5.1
・microsoft.powershell.utility モジュール 概要
https://learn.microsoft.com/ja-jp/powershell/module/microsoft.powershell.utility/?view=windowsserver2022-ps
補足：日本語訳が機械翻訳の頁は英語原文（上記URL）を正とします。社内既存ナレッジ（C:\oskb PS51-WS2022 4,030件）とも整合。
