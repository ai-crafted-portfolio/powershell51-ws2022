# Windows 研修資料 No.29 TCP/IP構成(NetTCPIP)

### Windows Server 2022
TCP/IP構成(NetTCPIP)
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29
凡例：・＝要点／－＝補足　｜　枠線囲みのMS Gothic＝コマンド・設定　｜　出典はMicrosoft Learn公式（末尾スライドにURL）。値・パスは標準既定の例で実環境確認が必要。

### TCP/IP構成(NetTCPIP)
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29　／　6. ネットワーク構成
この研修シリーズでの位置づけ
・1. サーバ管理基盤
・2. Active Directoryドメインサービス
・3. 認証・証明書・フェデレーション
・4. グループポリシー
・5. DNS/DHCP/IPAM
・▶ 6. ネットワーク構成　（この項目が属する分類）
・7. SDN・ネットワーク仮想化
・8. ストレージ
・9. 分散ファイル・レプリケーション
・10. クラスタリング・可用性
・11. Hyper-V仮想化
・12. リモートデスクトップ・アプリ配信
・13. Web/アプリ・配置基盤
・14. セキュリティ・ハードニング
・15. バックアップ・保守・診断

### TCP/IP構成(NetTCPIP)
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29　／　6. ネットワーク構成
扱う対象と前提
IP・NIC・チーミング・ファイアウォール・リモート接続など、サーバーのネットワーク設定全般を扱う分類です。
IPアドレス/ルート/近隣を管理。IPv4/IPv6。
前提
・該当する役割/機能をサーバーマネージャーまたは Install-WindowsFeature で導入済みであること。
・管理コマンドは nettcpip モジュール（役割導入時に併せて追加）を使用する。
・操作は原則 PowerShell（管理者）で実行。GUIツールがある場合は併記。

### TCP/IP構成(NetTCPIP)
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29　／　6. ネットワーク構成
想定作業（この項目で何をするか）
・IP設計・疎通の確認
・標準的な作業の流れ（参考）：
－現状の設定・構成を確認する（次スライドのコマンド・確認キー）
－必要に応じて作成・変更・削除など目的の操作を行う
－変更後は反映・動作を検証し、記録する
使用判断のポイント
この設定・機能が実環境で使われているかは、下記の確認キーで判断します。使っていなければ研修採用の要否を検討します。

### TCP/IP構成(NetTCPIP)
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29　／　6. ネットワーク構成
主要コマンド（例）
PS C:\> Get-NetIPAddress
PS C:\> New-NetIPAddress
PS C:\> Get-NetRoute
PS C:\> Test-NetConnection
確認する値・キー（使用判断）
・Get-NetIPAddress
・New-NetIPAddress
・Get-NetRoute
・Test-NetConnection

### TCP/IP構成(NetTCPIP)
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29　／　6. ネットワーク構成
この項目で使う主なコマンドレット（抜粋）
コマンドレット
主な用途
Get-NetIPAddress
取得・確認
New-NetIPAddress
新規作成
Get-NetRoute
取得・確認
Test-NetConnection
テスト・検証
Find-NetRoute
検索
Remove-NetIPAddress
削除
Set-NetIPAddress
設定・変更
Get-NetCompartment
取得・確認
Get-NetIPConfiguration
取得・確認
Get-NetIPInterface
取得・確認
Get-NetIPv4Protocol
取得・確認

### TCP/IP構成(NetTCPIP)（続き）
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29　／　6. ネットワーク構成
コマンドレット
主な用途
Get-NetIPv6Protocol
取得・確認
Get-NetNeighbor
取得・確認
Get-NetOffloadGlobalSetting
取得・確認
補足：上表はモジュールの代表的なコマンドレットの抜粋です。全体は Get-Command -Module <モジュール名> で確認できます。

### TCP/IP構成(NetTCPIP)
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29　／　6. ネットワーク構成
定義ファイル・場所（早見）
場所・ファイル・モジュール（標準既定の例）
確認
nettcpip モジュール
実環境で要確認
補足：パス・既定値はバージョン/構成で異なります。必ず実環境で確認してください。

### TCP/IP構成(NetTCPIP)
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29　／　6. ネットワーク構成
用語集
用語
区分・変更可否（既定値）
説明
nettcpip
モジュール名（固定）
本項目の操作を提供するモジュール。Import-Module nettcpip で利用。
モジュール
配布単位（固定名）
関連コマンドレットをまとめた配布単位。Import-Moduleで読み込む。
Get-NetIPAddress
コマンドレット
本項目で使用する主要コマンドレット。Get-Help Get-NetIPAddress で詳細。
New-NetIPAddress
コマンドレット
本項目で使用する主要コマンドレット。Get-Help New-NetIPAddress で詳細。

### TCP/IP構成(NetTCPIP)
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29　／　6. ネットワーク構成
出典（Microsoft Learn 公式資料）
・Get-NetIPAddress
https://learn.microsoft.com/ja-jp/powershell/module/nettcpip/get-netipaddress/?view=windowsserver2022-ps
・New-NetIPAddress
https://learn.microsoft.com/ja-jp/powershell/module/nettcpip/new-netipaddress/?view=windowsserver2022-ps
・Get-NetRoute
https://learn.microsoft.com/ja-jp/powershell/module/nettcpip/get-netroute/?view=windowsserver2022-ps
・Test-NetConnection
https://learn.microsoft.com/ja-jp/powershell/module/nettcpip/test-netconnection/?view=windowsserver2022-ps
・nettcpip モジュール 概要
https://learn.microsoft.com/ja-jp/powershell/module/nettcpip/?view=windowsserver2022-ps

### TCP/IP構成(NetTCPIP)（続き）
6. ネットワーク構成　>　IP　>　TCP/IP
Windows Server 2022 運用　No.29　／　6. ネットワーク構成
補足：日本語訳が機械翻訳の頁は英語原文（上記URL）を正とします。社内既存ナレッジ（C:\oskb PS51-WS2022 4,030件）とも整合。
