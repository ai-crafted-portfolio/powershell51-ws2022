# Windows 研修資料 No.28 IPアドレス管理(IPAM)

### Windows Server 2022
IPアドレス管理(IPAM)
5. DNS/DHCP/IPAM　>　IPAM　>　IPアドレス管理
Windows Server 2022 運用　No.28
凡例：・＝要点／－＝補足　｜　枠線囲みのMS Gothic＝コマンド・設定　｜　出典はMicrosoft Learn公式（末尾スライドにURL）。値・パスは標準既定の例で実環境確認が必要。

### IPアドレス管理(IPAM)
5. DNS/DHCP/IPAM　>　IPAM　>　IPアドレス管理
Windows Server 2022 運用　No.28　／　5. DNS/DHCP/IPAM
この研修シリーズでの位置づけ
・1. サーバ管理基盤
・2. Active Directoryドメインサービス
・3. 認証・証明書・フェデレーション
・4. グループポリシー
・▶ 5. DNS/DHCP/IPAM　（この項目が属する分類）
・6. ネットワーク構成
・7. SDN・ネットワーク仮想化
・8. ストレージ
・9. 分散ファイル・レプリケーション
・10. クラスタリング・可用性
・11. Hyper-V仮想化
・12. リモートデスクトップ・アプリ配信
・13. Web/アプリ・配置基盤
・14. セキュリティ・ハードニング
・15. バックアップ・保守・診断

### IPアドレス管理(IPAM)
5. DNS/DHCP/IPAM　>　IPAM　>　IPアドレス管理
Windows Server 2022 運用　No.28　／　5. DNS/DHCP/IPAM
扱う対象と前提
名前解決とIPアドレス配布というネットワークの根幹サービスと、その統合管理を扱う分類です。
ipamserverでDNS/DHCP/IPアドレス空間を統合管理・監査。
前提
・該当する役割/機能をサーバーマネージャーまたは Install-WindowsFeature で導入済みであること。
・管理コマンドは ipamserver モジュール（役割導入時に併せて追加）を使用する。
・操作は原則 PowerShell（管理者）で実行。GUIツールがある場合は併記。

### IPアドレス管理(IPAM)
5. DNS/DHCP/IPAM　>　IPAM　>　IPアドレス管理
Windows Server 2022 運用　No.28　／　5. DNS/DHCP/IPAM
想定作業（この項目で何をするか）
・IP管理の集中化有無確認
・標準的な作業の流れ（参考）：
－現状の設定・構成を確認する（次スライドのコマンド・確認キー）
－必要に応じて作成・変更・削除など目的の操作を行う
－変更後は反映・動作を検証し、記録する
使用判断のポイント
この設定・機能が実環境で使われているかは、下記の確認キーで判断します。使っていなければ研修採用の要否を検討します。

### IPアドレス管理(IPAM)
5. DNS/DHCP/IPAM　>　IPAM　>　IPアドレス管理
Windows Server 2022 運用　No.28　／　5. DNS/DHCP/IPAM
主要コマンド（例）
PS C:\> Get-IpamAddress
PS C:\> Get-IpamRange
確認する値・キー（使用判断）
・Get-IpamAddress
・Get-IpamRange

### IPアドレス管理(IPAM)
5. DNS/DHCP/IPAM　>　IPAM　>　IPアドレス管理
Windows Server 2022 運用　No.28　／　5. DNS/DHCP/IPAM
この項目で使う主なコマンドレット（抜粋）
コマンドレット
主な用途
Get-IpamAddress
取得・確認
Get-IpamRange
取得・確認
Add-IpamAddress
追加
Disable-IpamCapability
無効化
Enable-IpamCapability
有効化
Export-IpamAddress
エクスポート
Find-IpamFreeAddress
検索
Import-IpamAddress
インポート
Invoke-IpamGpoProvisioning
実行
Move-IpamDatabase
移動
Remove-IpamAddress
削除

### IPアドレス管理(IPAM)（続き）
5. DNS/DHCP/IPAM　>　IPAM　>　IPアドレス管理
Windows Server 2022 運用　No.28　／　5. DNS/DHCP/IPAM
コマンドレット
主な用途
Rename-IpamCustomField
名前変更
Set-IpamAccessScope
設定・変更
Update-IpamServer
更新
補足：上表はモジュールの代表的なコマンドレットの抜粋です。全体は Get-Command -Module <モジュール名> で確認できます。

### IPアドレス管理(IPAM)
5. DNS/DHCP/IPAM　>　IPAM　>　IPアドレス管理
Windows Server 2022 運用　No.28　／　5. DNS/DHCP/IPAM
定義ファイル・場所（早見）
場所・ファイル・モジュール（標準既定の例）
確認
ipamserver モジュール
実環境で要確認
補足：パス・既定値はバージョン/構成で異なります。必ず実環境で確認してください。

### IPアドレス管理(IPAM)
5. DNS/DHCP/IPAM　>　IPAM　>　IPアドレス管理
Windows Server 2022 運用　No.28　／　5. DNS/DHCP/IPAM
用語集
用語
区分・変更可否（既定値）
説明
ipamserver
モジュール名（固定）
本項目の操作を提供するモジュール。Import-Module ipamserver で利用。
モジュール
配布単位（固定名）
関連コマンドレットをまとめた配布単位。Import-Moduleで読み込む。
DNS
役割（固定名）
名前解決サービス。ゾーンとレコードで構成。
DHCP
役割（固定名）
IPアドレス自動配布サービス。スコープで範囲を定義。
IPAM
機能（固定名）
IPアドレス管理。DNS/DHCP/アドレス空間を統合管理。
Get-IpamAddress
コマンドレット
本項目で使用する主要コマンドレット。Get-Help Get-IpamAddress で詳細。
Get-IpamRange
コマンドレット
本項目で使用する主要コマンドレット。Get-Help Get-IpamRange で詳細。

### IPアドレス管理(IPAM)
5. DNS/DHCP/IPAM　>　IPAM　>　IPアドレス管理
Windows Server 2022 運用　No.28　／　5. DNS/DHCP/IPAM
出典（Microsoft Learn 公式資料）
・Get-IpamAddress
https://learn.microsoft.com/ja-jp/powershell/module/ipamserver/get-ipamaddress/?view=windowsserver2022-ps
・Get-IpamRange
https://learn.microsoft.com/ja-jp/powershell/module/ipamserver/get-ipamrange/?view=windowsserver2022-ps
・ipamserver モジュール 概要
https://learn.microsoft.com/ja-jp/powershell/module/ipamserver/?view=windowsserver2022-ps
補足：日本語訳が機械翻訳の頁は英語原文（上記URL）を正とします。社内既存ナレッジ（C:\oskb PS51-WS2022 4,030件）とも整合。
